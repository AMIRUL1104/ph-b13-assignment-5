- - 1️⃣ What is the difference between var, let, and const?
    Ans : var is a functional scope . that means we can access and update this variable only inside the function . let and const are blocked scope . we can access them only inside the block {} . let variable value are changeable but const variable valeu are not changeable .
- - 2️⃣ What is the spread operator (...)?
    ans : spread operator is a destructureing method . we can easily destructure object and array using spread operator .
- - 3️⃣ What is the difference between map(), filter(), and forEach()?
    Ans :
    map() : map is a immutable array method . it return a new array .
    filter() : it work on each element of an array . if the are match with the condition it wel return it . it make a new array . it is immutable .
    forEach() : this method also work on each element of an array . but it not return a new array . it can be mutable or immutable .

- - 4️⃣ What is an arrow function?
    arrow function is simplified version of the normal function .

  - 5️⃣ What are template literals?
    Ans : templete literals is a advanced features in javascript . by using it we can create multiline string . templete literal are writting by backticks (` `) . in templete litarel we can easily use variables , function etc .

  ***

## 📤 What to submit

- **GitHub Repository Link:**
- **Live Site Link:**

---
